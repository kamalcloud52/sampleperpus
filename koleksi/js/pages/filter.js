// ==================== FILTER PAGE ====================
import { openModal } from '../components/modal.js';

export function renderFilter(main) {
    setTimeout(() => {
        openModal('modalFilter');
    }, 100);
}
