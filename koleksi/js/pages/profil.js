// ==================== PROFIL PAGE ====================
import { openModal } from '../components/modal.js';

export function renderProfil(main) {
    setTimeout(() => {
        openModal('modalProfil');
    }, 100);
}
